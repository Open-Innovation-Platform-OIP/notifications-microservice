apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: oip-notifications-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-staging"
spec:
  tls:
  - hosts:
    - oip-notifications.k8s.jaagalabs.com
    secretName: oip-notifications-tls
  rules:  
  - host: oip-notifications.k8s.jaagalabs.com
    http:
      paths:
      - backend:
          serviceName: oip-notifications
          servicePort: 80